name: Deploy Documentation

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

permissions:
  contents: write

concurrency:
  group: healtharchive-pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout backend
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          path: healtharchive
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405
        with:
          python-version: "3.11"

      - name: Install dependencies
        working-directory: healtharchive
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev,docs]"

      - name: Generate API and assistant context
        working-directory: healtharchive
        run: |
          PYTHONPATH=src python3 scripts/export_openapi.py
          python3 scripts/generate_llms_txt.py

      - name: Build Documentation
        working-directory: healtharchive
        run: make docs-build

      - name: Docs references (advisory)
        working-directory: healtharchive
        continue-on-error: true
        run: make docs-refs

      - name: Docs coverage (advisory)
        working-directory: healtharchive
        continue-on-error: true
        run: make docs-coverage

      - name: Lychee Link Checker
        uses: lycheeverse/lychee-action@8646ba30535128ac92d33dfc9133794bfdd9b411
        with:
          args: --verbose --no-progress --exclude-mail "healtharchive/site/**/*.html"
          fail: false # Set to true if you want to block on dead links

      - name: Publish site to gh-pages
        working-directory: healtharchive
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail

          repo_url="https://github.com/${GITHUB_REPOSITORY}.git"
          auth_header="$(printf 'x-access-token:%s' "${GH_TOKEN}" | base64 | tr -d '\n')"
          publish_dir="$(mktemp -d)"

          if git -c "http.https://github.com/.extraheader=AUTHORIZATION: basic ${auth_header}" \
            clone --branch gh-pages --single-branch "${repo_url}" "${publish_dir}" >/dev/null 2>&1; then
            :
          else
            git init "${publish_dir}" >/dev/null 2>&1
            git -C "${publish_dir}" checkout --orphan gh-pages
          fi

          find "${publish_dir}" -mindepth 1 -maxdepth 1 ! -name '.git' -exec rm -rf {} +
          cp -a site/. "${publish_dir}/"
          touch "${publish_dir}/.nojekyll"

          git -C "${publish_dir}" config user.name "${GITHUB_ACTOR}"
          git -C "${publish_dir}" config user.email "${GITHUB_ACTOR}@users.noreply.github.com"
          git -C "${publish_dir}" add --all

          if git -C "${publish_dir}" diff --cached --quiet; then
            echo "No documentation changes to publish."
            exit 0
          fi

          git -C "${publish_dir}" commit -m "docs: publish ${GITHUB_SHA}"
          git -C "${publish_dir}" \
            -c "http.https://github.com/.extraheader=AUTHORIZATION: basic ${auth_header}" \
            push "${repo_url}" gh-pages:gh-pages
