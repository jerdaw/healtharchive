diff --git a/node_modules/lighthouse/node_modules/chrome-launcher/dist/chrome-launcher.js b/node_modules/lighthouse/node_modules/chrome-launcher/dist/chrome-launcher.js
index d7fb4cf..73a7999 100644
--- a/node_modules/lighthouse/node_modules/chrome-launcher/dist/chrome-launcher.js
+++ b/node_modules/lighthouse/node_modules/chrome-launcher/dist/chrome-launcher.js
@@ -117,7 +117,8 @@ class Launcher {
         if (!this.useDefaultProfile) {
             // Place Chrome profile in a custom location we'll rm -rf later
             // If in WSL, we need to use the Windows format
-            flags.push(`--user-data-dir=${isWsl ? toWin32Path(this.userDataDir) : this.userDataDir}`);
+            const useWindowsWslPath = isWsl && process.env.HEALTHARCHIVE_LINUX_CHROME !== '1';
+            flags.push(`--user-data-dir=${useWindowsWslPath ? toWin32Path(this.userDataDir) : this.userDataDir}`);
         }
         if (process.env.HEADLESS)
             flags.push('--headless');
diff --git a/node_modules/lighthouse/node_modules/chrome-launcher/dist/utils.js b/node_modules/lighthouse/node_modules/chrome-launcher/dist/utils.js
index 038d0dc..8ee7712 100644
--- a/node_modules/lighthouse/node_modules/chrome-launcher/dist/utils.js
+++ b/node_modules/lighthouse/node_modules/chrome-launcher/dist/utils.js
@@ -60,6 +60,8 @@ export function makeTmpDir() {
         case 'linux':
             return makeUnixTmpDir();
         case 'wsl':
+            if (process.env.HEALTHARCHIVE_LINUX_CHROME === '1')
+                return makeUnixTmpDir();
             // We populate the user's Windows temp dir so the folder is correctly created later
             process.env.TEMP = getWSLLocalAppDataPath(`${process.env.PATH}`);
         case 'win32':
